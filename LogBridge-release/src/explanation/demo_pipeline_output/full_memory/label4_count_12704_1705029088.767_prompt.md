You are an Apache IoTDB site reliability engineer. A monitoring window has been
flagged by our classifier. Produce a concise root-cause analysis and action plan.

# Window Context
- Time range: 2024-01-12T03:11:28.767000+00:00 → 2024-01-12T03:11:28.777000+00:00
- Log volume: 100 raw → 7 unique-template entries (compressed)
- Classifier prediction: **full_memory**
- Predicted-class confidence note: N/A (logits hook not wired in this scaffold)

# Top log templates (by within-window count)
  [cluster_3 x49] 2024-01-12 <*> <*> INFO <*> - <*> <*> <*> bytes, total memory size: <*> bytes.
  [cluster_8 x19] 2024-01-12 <*> <*> INFO org.apache.iotdb.consensus.iot.logdispatcher.LogDispatcher:163 - <*> Push a log to the queue, where the queue length is <*>
  [cluster_7 x8] 2024-01-12 <*> <*> INFO org.apache.iotdb.consensus.iot.service.IoTConsensusRPCServiceProcessor:125 - execute TSyncLogEntriesReq for TConsensusGroupId(type:DataRegion, <*> with result [TSStatus(code:20
  [cluster_5 x6] 2024-01-12 <*> <*> INFO org.apache.iotdb.consensus.iot.logdispatcher.LogDispatcher$LogDispatcherThread:360 - <*> startIndex: <*> maxIndex: <*> pendingEntries size: <*> bufferedEntries size: <*>
  [cluster_9 x6] 2024-01-12 <*> <*> INFO <*> - DataRegion[1] : accumulated a <*> <*> <*> <*> from <*> <*> <*>

# Frequent entities in this window
data_regions (x7), nodes (x7), consensus_groups (x7), statistical_entities (x7), node_ips (x7), thread (x7), extra_threads (x7)

# Retrieved expert knowledge (top-5 relevant tuples)
The classifier's prediction was used as a filter; these tuples come from IoTDB
docs / status codes / config reference / closed GitHub issues.

## Tuple 1: `CFG-sort_buffer_size_in_bytes` (sim=0.870)
- Source: docs_curated/Reference/Common-Config-Manual.md
- Title: Config: sort_buffer_size_in_bytes
- Symptom: 
- Root cause: The memory for external sort in sort operator, when the data size is smaller than sort_buffer_size_in_bytes, the sort operator will use in-memory sort.
- Fix:
      Default: 1048576(Before V2.0.6) <br> 0(Supports from V2.0.6), if `sort_buffer_size_in_bytes <= 0`, default value will be used, `default value = min(32MB, memory for query operators / query_thread_count / 2)`, if `sort_buffer_size_in_bytes > 0`, the specified value will be used.; Effective: Hot_reload; Tune via SET CONFIGURATION 'sort_buffer_size_in_bytes'='<value>' if hot-reloadable, else iotdb-system.properties + restart
- Anomaly classes: full_memory
- Subsystems: Memory

## Tuple 2: `DM-DR-2` (sim=0.869)
- Source: docs_curated/Deployment-and-Maintenance/Environment-Requirements.md
- Title: Sizing memory by number of timeseries
- Symptom: OOM during MemTable flush; chronic high GC time%; queries fail with 'not enough memory'
- Root cause: Under-provisioned heap vs metadata + MemTable + cache + query temp; ConfigNode also holds partition table proportional to series count
- Fix:
      Recommended memory per node (1Hz workload):
        ≤100K series → 2-4 GB
        ≤300K → 6-12 GB
        ≤500K → 12-24 GB
- Anomaly classes: full_memory
- Subsystems: Memory, MemTable, GC

## Tuple 3: `MON-5` (sim=0.860)
- Source: docs_curated/Tools-System/Monitor-Tool_apache.md
- Title: System Monitoring dashboard: JVM GC time % and heap memory usage
- Symptom: GC time percentage > 10%; Old-Gen approaches max heap; Full GC frequency rises
- Root cause: Heap too small for workload; MemTable + query temp + cache evicts too slowly; possible memory leak in plugin/UDF
- Fix:
      Increase IOTDB_HEAP_SIZE (datanode-env.sh)
      Reduce write_memory_proportion if read-heavy, or chunk_timeseriesmeta_free_memory_proportion if memory-pressured queries
      Check user-defined UDFs / triggers for leaks via jcmd / heap dump
- Anomaly classes: full_memory
- Subsystems: GC, Memory, MemTable, Cache

## Tuple 4: `FAQ-1.10` (sim=0.857)
- Source: docs_curated/FAQ/Frequently-asked-questions.md
- Title: How to deal with estimated out of memory errors?
- Symptom: Error 301: There is not enough memory to execute current fragment instance, current remaining free memory is X, estimated memory usage for current fragment instance is Y
- Root cause: Memory available for query execution (heap × datanode_memory_proportion × chunk_timeseriesmeta_free_memory_proportion = 30% × 20% = 6% of heap) is less than the query's estimated execution memory requirement. By default a 4G heap leaves only ~240MB for query execution.
- Fix:
      Increase IOTDB_HEAP_SIZE to >4.2G (so 4.2G * 30% * 20% > 257MB)
      Tune datanode_memory_proportion to allocate more memory to queries
      Reduce the number of exported time series in the query
      Add 'slimit N' clause to limit number of timeseries per query
- Anomaly classes: full_memory
- Subsystems: Memory, Query

## Tuple 5: `CFG-udf_initial_byte_array_length_for_memory_control` (sim=0.849)
- Source: docs_curated/Reference/Common-Config-Manual.md
- Title: Config: udf_initial_byte_array_length_for_memory_control
- Symptom: 
- Root cause: Used to estimate the memory usage of text fields in a UDF query. It is recommended to set this value to be slightly larger than the average length of all texts.
- Fix:
      Default: 48; Effective: After restarting system; Tune via SET CONFIGURATION 'udf_initial_byte_array_length_for_memory_control'='<value>' if hot-reloadable, else iotdb-system.properties + restart
- Anomaly classes: full_memory
- Subsystems: Memory, Query


# Output instructions
Write a report with these sections (markdown):

1. **Summary** — one sentence: what likely happened.
2. **Evidence alignment** — which 1-2 retrieved tuples best match the window's symptoms; quote their `id`.
3. **Root cause hypothesis** — be explicit about the IoTDB subsystem (e.g. Compaction / Flush / WAL).
4. **Recommended actions** — concrete commands or config changes, ordered safest first.
5. **What to monitor next** — which metric/dashboard panel confirms recovery.

Keep total length under 400 words. Cite tuple IDs inline like `(see MON-3)`.
